import es from "../../messages/es.json";

/**
 * Textos de UI. v1 sirve un solo idioma; la estructura ya separa
 * contenido de componentes para el multiidioma futuro (Fase 8).
 */
export const t = es;
export type Messages = typeof es;
